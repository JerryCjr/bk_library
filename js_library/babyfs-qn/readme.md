# babyfs-qn

>

## Build Setup

``` bash
# install dependencies
npm install

# serve development program
npm run dev

# build for production
npm run prod
```


## How to use
``` bash
const babyfsQN=require('babyfs-qn');
try{
  babyfsQN(stats,cb()=>{});
}catch(err){
  console.error(err);
}
```
