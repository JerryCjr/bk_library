# babyfs-wxapp-dependency-parser

> miniprogram npm dependency parser from node_modules directory

## Build Setup

``` bash
# install dependencies
npm install

# serve development program
npm run dev

# build for production
npm run prod
```
